version: 0.2

phases:
  pre_build:
    commands:
      - echo "=== Phase Pre-build - Transform Pricing MCP Server and ECR Login ==="
      - chmod +x transform-pricing.sh
      - bash ./transform-pricing.sh
      - echo "Logging in to Amazon ECR..."
      - aws ecr get-login-password --region $AWS_DEFAULT_REGION | docker login --username AWS --password-stdin $AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com

  build:
    commands:
      - echo "=== Phase Build - Docker ARM64 image ==="
      - cd mcp/src/aws-pricing-mcp-server
      - echo "Building Docker image..."
      - docker build --no-cache -t $ECR_REPO_URI:$CODEBUILD_BUILD_NUMBER .
      - docker tag $ECR_REPO_URI:$CODEBUILD_BUILD_NUMBER $ECR_REPO_URI:latest
      - echo "Docker image built and tagged."

  post_build:
    commands:
      - echo "=== Phase Post-build - Push to ECR ==="
      - docker push $ECR_REPO_URI:$CODEBUILD_BUILD_NUMBER
      - docker push $ECR_REPO_URI:latest
      - echo "Pricing MCP image pushed successfully."
